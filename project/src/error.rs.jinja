//! Error definition of {{project_name}}

/* std use */

/* crate use */

/* project use */

/// Enum to define error
{% if bin -%}
#[derive(std::fmt::Debug, thiserror::Error)]
pub enum Error {
    #[error(transparent)]
    Log(#[from] log::SetLoggerError),
}
{%- else -%}
#[derive(std::fmt::Debug, thiserror::Error)]
pub enum Error {}
{%- endif %}

/// Alias of result
{% if bin -%}
pub type Result<T> = anyhow::Result<T>;
{%- else -%}
pub type Result<T> = core::result::Result<T, Error>;
{%- endif %}
